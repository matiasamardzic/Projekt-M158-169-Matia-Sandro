# Projekt-M158-169-Matia-Sandro
Dieses Repository enthält die vollständige Infrastruktur als Code (IaC) für die lokale Migration einer Moodle Instanz. Das Setup stellt eine On-Premise-Umgebung bereit, die zwingend auf isolierten Docker-Containern für den Webserver und die MariaDB-Datenbank basiert.
